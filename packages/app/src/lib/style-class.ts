/**
 * TYPES.
 *
 * @description File for set core types.
 */

const pageContent    = 'm-8 flex items-center justify-center'
const pageContentRow = pageContent + ' flex-row'
const pageContentCol = pageContent + ' flex-col'
const fontColor      = 'text-gray-800 dark:text-gray-50'
const sectionBorder  = 'rounded-xl border-gray-800 border-[1px]'
const h1Super        = 'font-bold text-2xl sm:text-3xl md:text-6xl bg-primary-500 text-black hover-change-hue'
const h1             = 'font-bold text-2xl sm:text-3xl md:text-5xl text-primary-500 hover-change-hue'
const h1White        = 'font-bold text-2xl sm:text-3xl md:text-5xl text-white hover-change-hue'

export const styleClass = {
	pageContentRow,
	h1,
	h1Super,
	h1White,
	pageContentCol,
	fontColor,
	sectionBorder,
}
